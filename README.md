# 🎥 Adeita Vichat - Video Chat Application

Современное веб-приложение для видеозвонков с поддержкой P2P и relay подключений.

## 🚀 Технологии

- **Frontend**: Solid.js + TypeScript
- **Backend**: Node.js + Express + Socket.IO
- **WebRTC**: Видео/аудио связь
- **Server**: Nginx + PM2

## ✨ Основные функции

- 🎥 Видео и аудио звонки
- 💬 Текстовый чат
- 🖥️ Демонстрация экрана
- 🔦 Фонарик (для мобильных)
- 🔒 Выбор типа подключения (P2P или через сервер)
- 📱 Адаптивный дизайн (мобильный и десктоп)
- 🤖 Telegram Mini App интеграция

## 📦 Установка

### Требования
- Node.js 18+
- npm или yarn
- PM2 (для production)

### Локальная разработка

```bash
# Клонировать репозиторий
git clone https://github.com/mench2/adeita.online.git
cd adeita.online

# Установить зависимости сервера
npm install

# Установить зависимости Solid.js
cd solid-app
npm install

# Запустить dev сервер Solid.js
npm run dev

# В другом терминале запустить Node.js сервер
cd ..
node server.js
```

Откройте http://localhost:5173

## 🚀 Деплой на сервер

### Автоматический деплой (GitHub Actions)

При push в `main` ветку автоматически запускается деплой:
1. Код загружается на сервер
2. Собирается Solid.js приложение
3. Файлы копируются в `public/`
4. PM2 перезапускает сервер

### Ручной деплой

```bash
# На сервере
ssh root@95.81.117.141
cd /var/www/adeita.online
./update.sh
```

Или пошагово:

```bash
cd /var/www/adeita.online
git pull
cd solid-app
npm install
npm run build
cd ..
./deploy-solid.sh
pm2 restart adeita
```

## 📁 Структура проекта

```
adeita.online/
├── server.js              # Node.js сервер (Socket.IO)
├── package.json           # Зависимости сервера
├── public/                # Собранный Solid.js (раздается Nginx)
│   ├── index.html
│   ├── assets/
│   └── icon/
├── solid-app/             # Исходники Solid.js
│   ├── src/
│   │   ├── App.tsx        # Главный компонент
│   │   ├── components/    # UI компоненты
│   │   ├── hooks/         # Custom hooks (WebRTC, Socket.IO)
│   │   ├── stores/        # State management
│   │   └── utils/         # Утилиты
│   ├── package.json
│   └── vite.config.ts
├── deploy-solid.sh        # Скрипт деплоя
├── update.sh              # Скрипт быстрого обновления
├── cleanup.sh             # Скрипт очистки
└── nginx.conf             # Конфигурация Nginx
```

## 🔧 Скрипты

### Разработка
```bash
cd solid-app
npm run dev        # Dev сервер с HMR
npm run build      # Production сборка
npm run preview    # Предпросмотр production сборки
```

### Деплой
```bash
./update.sh        # Полное обновление (git pull + build + deploy)
./deploy-solid.sh  # Только копирование файлов
./cleanup.sh       # Очистка временных файлов
```

### PM2
```bash
pm2 status         # Статус процессов
pm2 logs adeita    # Логи
pm2 restart adeita # Перезапуск
pm2 monit          # Мониторинг
```

## 🌐 Nginx конфигурация

Nginx раздает статические файлы из `public/` и проксирует WebSocket на Node.js:

- Статика: `public/` → Nginx (быстро, с кешированием)
- Socket.IO: `/socket.io/` → Node.js:3001 (WebSocket)
- API: `/deploy` → Node.js:3001

## 🔒 Типы подключения

### P2P (Секретное)
- Прямое соединение между участниками
- Использует только STUN серверы
- Быстрее, но может не работать через строгие NAT

### Relay (Через сервер)
- Использует STUN + TURN серверы
- Работает всегда, даже через строгие NAT
- Немного медленнее, использует трафик сервера

Тип подключения выбирается **перед** созданием комнаты и передается через URL всем участникам.

## 📊 Мониторинг

```bash
# PM2 статус
pm2 status

# Логи в реальном времени
pm2 logs adeita

# Использование ресурсов
pm2 monit

# Nginx логи
tail -f /var/log/nginx/adeita_access.log
tail -f /var/log/nginx/adeita_error.log
```

## 🐛 Troubleshooting

### Сайт не загружается
```bash
pm2 status                    # Проверить PM2
systemctl status nginx        # Проверить Nginx
lsof -i :3001                 # Проверить порт 3001
```

### WebSocket не подключается
```bash
pm2 logs adeita               # Проверить логи
nginx -t                      # Проверить конфигурацию Nginx
```

### Старая версия загружается
```
Ctrl+Shift+R                  # Очистить кеш браузера
```

## 📝 Переменные окружения

Создайте `.env` файл (опционально):

```env
PORT=3001
NODE_ENV=production
```

## 🤝 Contributing

1. Fork репозиторий
2. Создайте feature ветку (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT

## 🔗 Ссылки

- 🌐 Сайт: https://adeita.online
- 🤖 Telegram Bot: [@AdeitaBot](https://t.me/AdeitaBot)
- 📱 Mini App: [Adeita Vichat](https://t.me/AdeitaBot/Adeita_Vichat)

## 👨‍💻 Автор

Разработано с ❤️ для удобных видеозвонков



