# 🚀 Команды для деплоя на сервере

## ⚠️ ВАЖНО: Выполняй эти команды на сервере по порядку!

### 1️⃣ Подключись к серверу:
```bash
ssh root@95.81.117.141
```

### 2️⃣ Перейди в директорию проекта:
```bash
cd /var/www/adeita.online
```

### 3️⃣ Получи изменения из Git:
```bash
git pull
```

### 4️⃣ Установи зависимости для Solid.js:
```bash
cd solid-app
npm install
```

### 5️⃣ Собери Solid.js приложение:
```bash
npm run build
```

### 6️⃣ Вернись в корень проекта:
```bash
cd ..
```

### 7️⃣ Запусти deploy скрипт:
```bash
chmod +x deploy-solid.sh
./deploy-solid.sh
```

### 8️⃣ Перезапусти PM2:
```bash
pm2 restart adeita
```

### 9️⃣ Проверь статус:
```bash
pm2 status
pm2 logs adeita --lines 50
```

### 🔟 (Опционально) Обнови Nginx конфигурацию:

**Только если у тебя проблемы с загрузкой или WebSocket!**

```bash
# Скопируй новую конфигурацию
cp /var/www/adeita.online/nginx.conf /etc/nginx/sites-available/adeita.online

# Проверь конфигурацию
nginx -t

# Если OK - перезапусти Nginx
systemctl reload nginx
```

---

## 🔍 Проверка работы:

1. Открой браузер: `https://adeita.online` (или твой домен)
2. Проверь что загружается Solid.js версия (в консоли браузера не должно быть ошибок)
3. Попробуй создать звонок
4. Проверь что работает переключатель типа подключения

---

## 🐛 Если что-то не работает:

### Проблема: Git pull не работает
```bash
# Сбрось локальные изменения
git reset --hard origin/main
git pull
```

### Проблема: npm install падает
```bash
# Очисти кеш npm
cd solid-app
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

### Проблема: Build падает
```bash
# Проверь версию Node.js (должна быть 18+)
node -v

# Если меньше 18, обнови:
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs
```

### Проблема: PM2 не перезапускается
```bash
# Полностью останови и запусти заново
pm2 stop adeita
pm2 delete adeita
pm2 start server.js --name adeita
pm2 save
```

### Проблема: Сайт не открывается
```bash
# Проверь что сервер запущен
pm2 status

# Проверь логи
pm2 logs adeita --lines 100

# Проверь что порт 3001 слушается
netstat -tlnp | grep 3001

# Если используешь Nginx, перезапусти его
systemctl restart nginx
```

---

## 📊 Полезные команды:

```bash
# Посмотреть логи в реальном времени
pm2 logs adeita

# Посмотреть использование ресурсов
pm2 monit

# Перезапустить при изменениях
pm2 restart adeita --watch

# Посмотреть детальную информацию
pm2 show adeita
```

---

## 🔄 Для будущих обновлений:

Создай алиас для быстрого деплоя:

```bash
# Добавь в ~/.bashrc
echo 'alias deploy-adeita="cd /var/www/adeita.online && git pull && ./deploy-solid.sh && pm2 restart adeita"' >> ~/.bashrc
source ~/.bashrc

# Теперь можно просто:
deploy-adeita
```

---

## ✅ Checklist после деплоя:

- [ ] `git pull` прошел успешно
- [ ] `npm install` в `solid-app/` завершился без ошибок
- [ ] `npm run build` создал файлы в `solid-app/dist/`
- [ ] `./deploy-solid.sh` скопировал файлы в `public/`
- [ ] `pm2 restart adeita` перезапустил сервер
- [ ] Сайт открывается в браузере
- [ ] Можно создать звонок
- [ ] Работает переключатель типа подключения
- [ ] Работает копирование ссылки
- [ ] Работает чат (на десктопе)

---

## 🎉 Готово!

Если все пункты выполнены - твой Solid.js сайт работает!

Проблемы? Пиши мне, разберемся! 🚀

