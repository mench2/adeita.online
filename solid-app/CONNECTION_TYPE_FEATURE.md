# 🔒 Функция выбора типа подключения

## Описание

Добавлена возможность выбора типа подключения **ДО** начала звонка.

**ВАЖНО:** Тип подключения передается в ссылке, чтобы все участники использовали **одинаковый** режим!

### 🌐 Через сервер (по умолчанию)
- Использует **STUN + TURN** серверы
- Надежнее, работает везде
- Работает даже за строгим NAT/Firewall
- Видео может идти через сервер-ретранслятор

### 🔒 Секретное подключение
- Использует **только STUN** серверы
- Прямое P2P соединение между устройствами
- Видео идет напрямую, минуя сервер
- Быстрее, но может не работать за некоторыми NAT/Firewall

## Как это работает

### 1. Создатель комнаты выбирает тип подключения
Перед началом звонка пользователь видит две кнопки:
- **🌐 Через сервер** - безопасный выбор (по умолчанию)
- **🔒 Секретное** - для прямого соединения

### 2. Создание ссылки с типом подключения
При копировании ссылки:
- Добавляется параметр `?room=abc123&direct=1` (для секретного)
- Или `?room=abc123` (для через сервер)
- В тексте указывается: "Тип подключения: 🔒 Секретное подключение (P2P)"

**Пример ссылок:**
```
Через сервер:
https://adeita.online/?room=abc123

Секретное:
https://adeita.online/?room=abc123&direct=1
```

### 3. Присоединение по ссылке
Когда пользователь открывает ссылку:
- Тип подключения **автоматически устанавливается** из URL
- Кнопки выбора **заблокированы** (нельзя изменить)
- Показывается: "Тип подключения (установлен создателем)"
- Все участники используют **одинаковый** режим

### 4. Создание соединения
При нажатии "Start a call" или "Join call":
- Используются выбранные ICE серверы
- Логируется тип соединения в консоль
- При установке соединения показывается тип кандидата:
  - `🏠 host` - локальное соединение (в одной сети)
  - `⚡ srflx` - STUN (прямое через NAT)
  - `🔄 relay` - TURN (через сервер)

## Технические детали

### Файлы изменены:

#### 1. `src/stores/appStore.ts`
```typescript
export const [useDirectConnection, setUseDirectConnection] = createSignal(false);
```

#### 2. `src/utils/webrtc.ts`
```typescript
// Разделение серверов
export const stunOnlyServers = [...]; // Только STUN
export const turnServers = [...];     // Только TURN
export const stunServers = [...];     // Все вместе

// Функция выбора
export function getIceServers(directOnly: boolean): RTCIceServer[] {
  return directOnly ? stunOnlyServers : stunServers;
}

// Обновленная конфигурация
export function createPeerConnectionConfig(
  participantCount: number, 
  directOnly: boolean = false
): RTCConfiguration {
  // ...
  iceServers: getIceServers(directOnly)
  // ...
}
```

#### 3. `src/hooks/useWebRTC.ts`
```typescript
export function createPeerConnection(peerId: string, localStream: MediaStream | null): Peer {
  const directOnly = appStore.useDirectConnection();
  const config = createPeerConnectionConfig(participantCount, directOnly);
  
  // Логирование типа соединения
  pc.onicecandidate = (e) => {
    if (e.candidate) {
      const candidateType = e.candidate.type;
      // host / srflx / relay
      console.log(`Тип: ${candidateType}`);
    }
  };
}
```

#### 4. `src/components/ConnectionTypeSelector.tsx` (новый)
Компонент с UI для выбора типа подключения.

#### 5. `src/styles.css`
Добавлены стили для `.connection-type-selector`.

#### 6. `src/App.tsx`
- Импорт и использование `<ConnectionTypeSelector />`
- Парсинг параметра `direct` из URL в `checkUrlParams()`
- Добавление параметра `direct` в ссылку в `copyRoomLink()`

```typescript
// Парсинг из URL
const directParam = urlParams.get('direct');
if (directParam === '1' || directParam === 'true') {
  appStore.setUseDirectConnection(true);
}

// Добавление в ссылку
const directParam = appStore.useDirectConnection() ? '&direct=1' : '';
const roomUrl = `${origin}?room=${roomId}${directParam}`;
```

## Отладка

### Проверка типа соединения в консоли:

```javascript
// При создании соединения:
Creating peer connection for abc123 with config: {...}
Connection mode: 🔒 Секретное (STUN only)
// или
Connection mode: 🌐 Через сервер (STUN + TURN)

// При получении ICE кандидатов:
🏠 Используется локальное соединение для abc123
⚡ Используется STUN (прямое через NAT) для abc123
🔄 Используется TURN сервер для abc123
```

### Как проверить что работает секретное подключение:

1. Выбери "🔒 Секретное"
2. Начни звонок
3. Открой консоль (F12)
4. Ищи строки с `⚡ Используется STUN` или `🏠 Используется локальное`
5. Если видишь `🔄 Используется TURN` - значит прямое соединение не удалось

## Когда использовать каждый режим

### Через сервер (рекомендуется):
- ✅ Корпоративные сети с firewall
- ✅ Мобильные сети (3G/4G/5G)
- ✅ Публичный WiFi
- ✅ Когда надежность важнее скорости
- ✅ Когда не уверен в настройках сети

### Секретное (для продвинутых):
- ✅ Домашняя сеть с хорошим роутером
- ✅ Прямое соединение в одной локальной сети
- ✅ Когда важна минимальная задержка
- ✅ Когда хочешь чтобы видео не шло через сервер
- ❌ Может не работать за некоторыми NAT

## Будущие улучшения (опционально)

1. **Автоматический fallback**
   ```typescript
   // Если STUN не работает → автоматически TURN
   if (pc.iceConnectionState === 'failed' && directOnly) {
     appStore.setUseDirectConnection(false);
     // Пересоздать соединение
   }
   ```

2. **Индикатор текущего типа соединения**
   ```tsx
   <div class="connection-status">
     {connectionType() === 'relay' ? '🔄 Через сервер' : '⚡ Прямое'}
   </div>
   ```

3. **Статистика соединения**
   ```typescript
   const stats = await pc.getStats();
   // Показать задержку, битрейт, потери пакетов
   ```

## FAQ

**Q: Что безопаснее?**
A: Оба варианта безопасны (WebRTC использует шифрование). "Секретное" просто означает что видео идет напрямую между устройствами.

**Q: Что быстрее?**
A: Секретное подключение обычно быстрее (меньше задержка), но не всегда возможно.

**Q: Можно ли переключить во время звонка?**
A: Нет, нужно выбрать до начала звонка. Переключение во время звонка требует пересоздания всех соединений.

**Q: Почему я не могу изменить тип подключения?**
A: Если вы присоединились по ссылке, тип подключения установлен создателем комнаты. Все участники должны использовать одинаковый режим.

**Q: Что если я хочу другой тип подключения?**
A: Попросите создателя комнаты создать новую ссылку с нужным типом подключения, или создайте свою комнату.

**Q: Почему "Секретное" не работает?**
A: Возможные причины:
- Строгий NAT/Firewall
- Symmetric NAT у обоих участников
- Заблокированы UDP порты
- Решение: используй "Через сервер"

**Q: Видит ли сервер мое видео при "Через сервер"?**
A: Сервер может ретранслировать поток, но он зашифрован end-to-end. Сервер не может расшифровать содержимое.

**Q: Как проверить какой тип подключения используется?**
A: Посмотри в URL - если есть `&direct=1`, значит секретное. Также в консоли будут логи с типом ICE кандидатов.

