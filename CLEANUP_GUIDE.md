# 🧹 РУКОВОДСТВО ПО ОЧИСТКЕ СТАРЫХ ФАЙЛОВ

## 🎯 Когда очищать?

**После успешного деплоя Solid.js версии** и проверки что все работает.

---

## ✅ **БЫСТРАЯ ОЧИСТКА (РЕКОМЕНДУЕТСЯ):**

```bash
# На сервере:
cd /var/www/adeita.online
./cleanup.sh
```

Этот скрипт безопасно удалит:
- ✅ Старый `deploy.sh` (если есть)
- ✅ `solid-app/dist/` (пересоздастся при сборке)
- ✅ Логи PM2
- ✅ Кеш npm

---

## 🗑️ **ЧТО МОЖНО УДАЛИТЬ:**

### 1. Старый deploy.sh
```bash
rm -f /var/www/adeita.online/deploy.sh
```
**Зачем**: Теперь используется `deploy-solid.sh`

---

### 2. solid-app/dist/
```bash
rm -rf /var/www/adeita.online/solid-app/dist/
```
**Зачем**: Это временная папка для сборки, пересоздается при `npm run build`

**Экономия**: ~2-5 MB

---

### 3. Логи PM2
```bash
pm2 flush
```
**Зачем**: Очищает накопившиеся логи

**Экономия**: Зависит от времени работы, может быть 10-100 MB

---

### 4. Кеш npm
```bash
npm cache clean --force
```
**Зачем**: Очищает кеш пакетов npm

**Экономия**: ~50-200 MB

---

### 5. solid-app/node_modules/ (ОПЦИОНАЛЬНО)
```bash
rm -rf /var/www/adeita.online/solid-app/node_modules/
```
**Зачем**: Экономия места

**Экономия**: ~150-300 MB

**⚠️ НО**: Придется заново делать `npm install` при следующем обновлении (займет 2-3 минуты)

**РЕКОМЕНДАЦИЯ**: **НЕ удалять**, оставить для быстрых обновлений

---

## 🚫 **ЧТО НЕ УДАЛЯТЬ (КРИТИЧНО):**

| Файл/Папка | Зачем нужен | Что будет если удалить |
|------------|-------------|------------------------|
| `server.js` | Node.js сервер | ❌ Сайт перестанет работать |
| `package.json` (корневой) | Зависимости server.js | ❌ PM2 не сможет запустить |
| `node_modules/` (корневой) | Модули для server.js | ❌ Сайт не запустится |
| `public/` | Рабочий сайт | ❌ Сайт не загрузится |
| `solid-app/src/` | Исходники | ❌ Не сможешь обновлять сайт |
| `solid-app/package.json` | Конфигурация сборки | ❌ Не соберется при обновлении |
| `deploy-solid.sh` | Скрипт деплоя | ❌ Придется копировать файлы вручную |

---

## 📊 **СКОЛЬКО МЕСТА ОСВОБОДИТСЯ:**

### Минимальная очистка (безопасная):
```bash
./cleanup.sh
```
**Освободится**: ~5-10 MB

---

### Максимальная очистка (с node_modules):
```bash
./cleanup.sh
rm -rf /var/www/adeita.online/solid-app/node_modules/
```
**Освободится**: ~200-400 MB

**⚠️ Но**: Следующий `npm install` займет 2-3 минуты

---

## 🔍 **ПРОВЕРКА МЕСТА НА ДИСКЕ:**

```bash
# Общее место
df -h

# Размер проекта
du -sh /var/www/adeita.online

# Размер по папкам
du -sh /var/www/adeita.online/*

# Самые большие папки
du -h /var/www/adeita.online | sort -rh | head -10
```

---

## 📋 **ОПТИМАЛЬНАЯ СТРУКТУРА (ПОСЛЕ ОЧИСТКИ):**

```
/var/www/adeita.online/              (~500 MB)
├── server.js                        (20 KB)
├── package.json                     (1 KB)
├── package-lock.json                (50 KB)
├── node_modules/                    (~150 MB) ✅ Оставить
├── public/                          (~5 MB) ✅ Оставить
│   ├── index.html
│   ├── assets/
│   └── icon/
├── solid-app/                       (~300 MB)
│   ├── src/                         (~100 KB) ✅ Оставить
│   ├── package.json                 (1 KB) ✅ Оставить
│   ├── package-lock.json            (50 KB) ✅ Оставить
│   ├── node_modules/                (~300 MB) ✅ Оставить для быстрых обновлений
│   ├── dist/                        🗑️ Удалено (пересоздастся)
│   └── ...
├── deploy-solid.sh                  (1 KB) ✅ Оставить
├── cleanup.sh                       (1 KB) ✅ Оставить
├── nginx.conf                       (2 KB) ✅ Оставить
└── *.md                             (~50 KB) ✅ Оставить (документация)
```

---

## 🔄 **РЕГУЛЯРНАЯ ОЧИСТКА:**

### Раз в месяц:
```bash
cd /var/www/adeita.online
./cleanup.sh
```

### Раз в 3 месяца:
```bash
cd /var/www/adeita.online
./cleanup.sh
# Если мало места:
rm -rf solid-app/node_modules/
cd solid-app && npm install
```

---

## 🆘 **ЕСЛИ СЛУЧАЙНО УДАЛИЛ ВАЖНОЕ:**

### 1. Восстановить через Git:
```bash
cd /var/www/adeita.online
git checkout HEAD -- <файл>
```

### 2. Восстановить node_modules:
```bash
# Корневой
cd /var/www/adeita.online
npm install

# Solid.js
cd /var/www/adeita.online/solid-app
npm install
```

### 3. Пересобрать Solid.js:
```bash
cd /var/www/adeita.online
./deploy-solid.sh
pm2 restart adeita
```

---

## ✅ **ЧЕКЛИСТ ПОСЛЕ ОЧИСТКИ:**

- [ ] Сайт открывается: `https://adeita.online`
- [ ] PM2 работает: `pm2 status`
- [ ] Логи без ошибок: `pm2 logs adeita --lines 20`
- [ ] Можно создать звонок
- [ ] Проверил место на диске: `df -h`

---

## 🎉 **ГОТОВО!**

После очистки:
- ✅ Освободили место
- ✅ Удалили старые файлы
- ✅ Сайт продолжает работать
- ✅ Можно быстро обновлять

**Рекомендация**: Запускай `./cleanup.sh` раз в месяц для поддержания чистоты! 🧹

