# ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ ДЕПЛОЯ

## 🎯 Что нужно сделать на сервере:

### 1. Подключись к серверу:
```bash
ssh root@95.81.117.141
```

### 2. Выполни деплой (копируй целиком):
```bash
cd /var/www/adeita.online && \
git pull && \
cd solid-app && \
npm install && \
npm run build && \
cd .. && \
chmod +x deploy-solid.sh && \
./deploy-solid.sh && \
pm2 restart adeita
```

### 3. Проверь логи:
```bash
pm2 logs adeita --lines 50
```

Должно быть примерно так:
```
Server running on port 3001
Socket.IO server started
```

### 4. Открой сайт:
```
https://adeita.online
```

---

## 🔍 Что проверить:

- [ ] Сайт открывается (не 502/504 ошибка)
- [ ] Нет ошибок в консоли браузера (F12)
- [ ] Можно нажать "Start a call"
- [ ] Появляется запрос на камеру/микрофон
- [ ] Видно переключатель типа подключения:
  - На мобильном: вверху справа (компактный)
  - На десктопе: внизу по центру (большой)
- [ ] Можно скопировать ссылку на комнату
- [ ] Работает чат (на десктопе)

---

## 🐛 Если что-то не так:

### Проблема 1: Сайт не открывается (502/504)
```bash
# Проверь что PM2 запущен
pm2 status

# Если не запущен
pm2 start server.js --name adeita
pm2 save

# Проверь что порт 3001 слушается
netstat -tlnp | grep 3001
```

### Проблема 2: Статические файлы не загружаются
```bash
# Проверь что файлы скопировались
ls -la /var/www/adeita.online/public/

# Должны быть:
# - index.html
# - assets/ (с .js и .css файлами)
# - icon/ (иконки)

# Если нет - запусти deploy заново
cd /var/www/adeita.online
./deploy-solid.sh
```

### Проблема 3: WebSocket не подключается
```bash
# Обнови Nginx конфигурацию
cp /var/www/adeita.online/nginx.conf /etc/nginx/sites-available/adeita.online

# Проверь синтаксис
nginx -t

# Если OK - перезапусти
systemctl reload nginx

# Проверь логи Nginx
tail -f /var/log/nginx/adeita.online.error.log
```

### Проблема 4: Старая версия загружается
```bash
# Очисти кеш браузера или открой в инкогнито
# Ctrl+Shift+R (Windows/Linux) или Cmd+Shift+R (Mac)

# Проверь что новые файлы на месте
cat /var/www/adeita.online/public/index.html | grep "solid"
```

---

## 📊 Полезные команды:

```bash
# Логи в реальном времени
pm2 logs adeita

# Использование ресурсов
pm2 monit

# Перезапуск
pm2 restart adeita

# Статус всех процессов
pm2 list

# Детальная информация
pm2 show adeita

# Логи Nginx
tail -f /var/log/nginx/adeita.online.access.log
tail -f /var/log/nginx/adeita.online.error.log
```

---

## 🎉 Если все работает:

**Поздравляю! Твой Solid.js сайт работает!** 🚀

Теперь для будущих обновлений просто:

```bash
ssh root@95.81.117.141
cd /var/www/adeita.online
git pull
./deploy-solid.sh
pm2 restart adeita
```

---

## 📝 Что изменилось:

### ✅ Новые фичи:
- Переключатель типа подключения (P2P vs через сервер)
- Улучшенная производительность
- Адаптивное качество видео
- Лучшая работа на мобильных
- Фиксы для iOS/Safari

### 🏗️ Архитектура:
- Миграция с vanilla JS на Solid.js
- Реактивное управление состоянием
- Оптимизированный рендеринг
- Batch операции для производительности

### 🔧 Технические улучшения:
- ResizeObserver для динамической сетки
- Debounce для уведомлений
- Мемоизация компонентов
- Правильная очистка ресурсов

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверь логи: `pm2 logs adeita`
2. Проверь консоль браузера (F12)
3. Проверь логи Nginx: `tail -f /var/log/nginx/adeita.online.error.log`
4. Напиши мне с описанием проблемы и логами

**Удачи с деплоем!** 💪

