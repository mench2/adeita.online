# 🛡️ БЕЗОПАСНЫЙ ДЕПЛОЙ - Ничего не сломается!

## ✅ **ЧТО ГАРАНТИРОВАННО НЕ СЛОМАЕТСЯ:**

### 1. **server.js** - НЕ меняется
- Продолжит работать как раньше
- Все зависимости остаются
- PM2 конфигурация не меняется

### 2. **Иконки и статические файлы**
- `public/icon/` будет сохранена
- Vite автоматически копирует все из `public/` в `dist/`
- Deploy скрипт просто заменяет содержимое `public/`

### 3. **Node.js зависимости для server.js**
- `package.json` (корневой) - не меняется
- `node_modules/` (корневой) - не меняется
- Все работает как раньше

---

## 📊 **ЧТО ИЗМЕНИТСЯ:**

### Было:
```
/var/www/adeita.online/
├── server.js
├── package.json
├── node_modules/
└── public/
    ├── index.html (старый, 3306 строк)
    └── icon/
```

### Станет:
```
/var/www/adeita.online/
├── server.js              ← НЕ МЕНЯЕТСЯ
├── package.json           ← НЕ МЕНЯЕТСЯ
├── node_modules/          ← НЕ МЕНЯЕТСЯ
├── public/                ← ОБНОВЛЯЕТСЯ
│   ├── index.html         ← Новый (Solid.js)
│   ├── assets/            ← Новая папка (JS/CSS)
│   └── icon/              ← Сохраняется (Vite копирует)
└── solid-app/             ← НОВАЯ ПАПКА
    ├── src/
    ├── dist/
    └── package.json       ← Отдельный для Solid.js
```

---

## 🔍 **Как работает deploy-solid.sh:**

```bash
#!/bin/bash

# 1. Переходим в solid-app
cd solid-app

# 2. Устанавливаем зависимости Solid.js (не влияет на server.js)
npm install

# 3. Собираем Solid.js → solid-app/dist/
npm run build
# Vite автоматически копирует public/icon/ в dist/icon/

# 4. Заменяем содержимое public/ на dist/
rm -rf ../public/*
cp -r dist/* ../public/
# Теперь в public/ есть: index.html, assets/, icon/
```

---

## ✅ **БЕЗОПАСНОСТЬ:**

### Что НЕ удаляется:
- ✅ `server.js`
- ✅ `package.json` (корневой)
- ✅ `node_modules/` (корневой)
- ✅ `deploy.sh` (старый скрипт)
- ✅ Любые другие файлы в корне

### Что заменяется:
- 🔄 `public/index.html` - старый → новый (Solid.js)
- 🔄 `public/icon/` - копируется из dist/ (та же самая)
- ➕ `public/assets/` - добавляется (JS/CSS)

### Что добавляется:
- ➕ `solid-app/` - новая папка с исходниками
- ➕ `deploy-solid.sh` - новый скрипт
- ➕ Документация (.md файлы)

---

## 🎯 **КОМАНДА ДЛЯ ДЕПЛОЯ:**

```bash
cd /var/www/adeita.online && \
git pull && \
cd solid-app && \
npm install && \
npm run build && \
cd .. && \
chmod +x deploy-solid.sh && \
./deploy-solid.sh && \
pm2 restart adeita
```

---

## 🔄 **Откат на старую версию (если что-то пойдет не так):**

### Вариант 1: Через Git
```bash
cd /var/www/adeita.online
git checkout HEAD~1 public/index.html
pm2 restart adeita
```

### Вариант 2: Полный откат
```bash
cd /var/www/adeita.online
git log --oneline  # Найди коммит до деплоя
git checkout <commit-hash> public/
pm2 restart adeita
```

### Вариант 3: Ручной откат
```bash
# Если сохранил бэкап
cp /path/to/backup/index.html /var/www/adeita.online/public/
pm2 restart adeita
```

---

## 📋 **ЧЕКЛИСТ ПЕРЕД ДЕПЛОЕМ:**

- [ ] Сделал бэкап `public/index.html` (на всякий случай)
- [ ] Проверил что PM2 работает: `pm2 status`
- [ ] Проверил что есть место на диске: `df -h`
- [ ] Проверил версию Node.js: `node -v` (должна быть 18+)

### Создать бэкап (опционально):
```bash
cd /var/www/adeita.online
cp -r public/ public_backup_$(date +%Y%m%d_%H%M%S)
```

---

## 🎉 **ГАРАНТИИ:**

1. ✅ **server.js не изменится** - продолжит работать
2. ✅ **PM2 не сломается** - та же конфигурация
3. ✅ **Иконки сохранятся** - Vite копирует их
4. ✅ **Можно откатить** - через Git или бэкап
5. ✅ **Зависимости изолированы** - `solid-app/` имеет свой `package.json`

---

## 💡 **ПОЧЕМУ ЭТО БЕЗОПАСНО:**

### Две отдельные части:
1. **Backend (server.js)**: 
   - Не меняется
   - Свой `package.json`
   - Свой `node_modules/`
   - Просто раздает файлы из `public/`

2. **Frontend (Solid.js)**:
   - Живет в `solid-app/`
   - Свой `package.json`
   - Свой `node_modules/`
   - Компилируется в `dist/` → копируется в `public/`

### Они не конфликтуют!
- `server.js` не знает что в `public/` - HTML или Solid.js
- Ему все равно, он просто раздает файлы
- Solid.js не влияет на работу `server.js`

---

## 🚀 **ГОТОВ К ДЕПЛОЮ!**

**Ничего не сломается, все безопасно!**

Копируй команду и запускай:

```bash
cd /var/www/adeita.online && \
git pull && \
cd solid-app && \
npm install && \
npm run build && \
cd .. && \
chmod +x deploy-solid.sh && \
./deploy-solid.sh && \
pm2 restart adeita
```

**Удачи!** 🎊

